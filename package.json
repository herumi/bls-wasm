{
  "name": "eth2-bls-wasm",
  "version": "0.0.1",
  "description": "BLS signature by WebAssembly",
  "main": "bls-eth-wasm/bls.js",
  "types": "./bls-wasm/bls.d.ts",
  "files": [
    "bls-eth-wasm"
  ],
  "scripts": {
    "prebuild": "rm -f bls-eth-wasm/bls_c.js && rm -f bls-eth-wasm/bls_c.wasm",
    "build": "cd bls && make clean && make bls-eth-wasm",
    "test": "node test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/herumi/bls-wasm.git"
  },
  "keywords": [
    "BLS signature",
    "WebAssembly",
    "pairing"
  ],
  "author": "herumi <herumi@nifty.com> (https://github.com/herumi/)",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/herumi/bls-wasm/issues"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "homepage": "https://github.com/herumi/bls-wasm#readme",
  "devDependencies": {
    "assert": "^2.0.0",
    "standard": "^10.0.3"
  }
}
